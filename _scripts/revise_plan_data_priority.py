from docx import Document
from pathlib import Path


def set_text(doc, idx, text):
    if idx < len(doc.paragraphs):
        doc.paragraphs[idx].text = text


def main():
    src = Path("方案_v2.9_模型2.5签约履约偏差_完整版修订.docx")
    dst = Path("方案_v2.9_模型2.5签约履约偏差_最终完整版.docx")
    doc = Document(str(src))

    updates = {
        11: "说明：DMP 线上导出数据为主表（173 字段全覆盖），中标报量补充投标阶段时间线字段。审计附表不再并行承担保证金、垫资等与 DMP/SAP 重复的主数据职责，仅补充 DMP 与 SAP 均缺失、但模型运行确有必要的少量状态认定字段。区域认定按年度分表，系统根据项目签约年份自动匹配对应年度认定表，确保“旧项目用旧认定、新项目用新认定”。",
        424: "核心底线校验（红档、垫资、优先权、现金类保证金、结算周期、非现金支付、付款比例、利润率等）以 DMP 字段和文本识别结果为前置判断依据。涉及应收未收、实际回收、逾期天数、实际资金占用结果等履约后事实数据，以 SAP 为准；审计附表不再重复承担与 DMP、SAP 同口径的保证金字段录入。",
        484: "模型2.3：保证金与预收款资金安全监控模型",
        486: "制度依据：各项目合同条款（保证金退还约定）、《融资管理办法》9.4（联合体保函管理）。其中合同约定类字段优先取 DMP，实际发生、回收和逾期类字段优先取 SAP。",
        504: "字段口径说明：现金类投标保证金标记、履约担保方式、预收款约定、联合体担保安排等前置判断字段由 DMP 覆盖；保证金实际回收日、预收款到账日、逾期天数等结果字段依赖 SAP 日期与余额字段。审计附表如与 SAP 存在同口径保证金字段，可剔除重复项，仅保留系统未覆盖的例外说明。",
        845: "**v2.9 精简说明**：较 v2.8 的 40 字段需求，客户台账、OA 审批流、附表 3、附表 6 等多项字段已移除或降级。当前对模型形成实质阻塞的外部字段，主要剩余 SAP 履约后日期类字段。保证金、垫资、担保方式等合同约定类字段，已优先由 DMP 承担；审计附表中的重复保证金字段可不再保留。",
        846: "6.3 审计附表不纳入或不再重复纳入字段",
        855: "11个模型统一使用",
        905: "**数据优先级说明**：系统按“DMP 优先约定口径、SAP 优先实际发生口径、审计附表仅作补充”的原则取数。凡 DMP 已有的合同约定类字段（如是否垫资、预估垫资金额、垫资比例、累计垫资周期、投标担保备注、履约担保方式等），不再要求审计附表重复填报；凡 SAP 已有的实际履约类字段（如实际产值、实际利润、累计收款、保证金回收日期、预收款到账与余额等），以 SAP 为准，审计附表同口径字段可剔除。审计附表仅保留 DMP 与 SAP 均缺失、但审计又必须保留的状态说明和例外认定字段。区域认定数据继续支持 2023-2026 四个年度并按签约年份自动匹配。",
        1012: "附录：待补充数据字段清单与保留原则",
        1013: "以下字段为当前 11 个模型中仍有明确校验需求、但现有主数据源中尚未完全覆盖，或需进一步明确优先级的字段。按缺失数据源分类列出，并同步说明哪些审计附表字段可被 DMP 或 SAP 替代。",
        1015: "A. 附表1（审计附表）保留补充字段",
        1016: "**修订原则**：审计附表不再补充与 DMP、SAP 同口径的保证金、垫资、预收款等重复字段。附表仅保留项目实际状态、停工/退场原因、三证实际取得明细、以及系统未覆盖的人工认定说明等补充字段。若附表与 SAP 存在相同的实际金额、实际回收、累计收款类字段，以 SAP 为准，附表可剔除重复列。",
        1032: "D. SAP/财务系统履约数据（需从财务系统导出）",
        1033: "影响模型2.1、2.2、2.3的履约后实际数据检测。SAP 重点承担实际产值、实际利润、累计收款、保证金实际回收、预收款到账与余额、逾期天数计算等事实类字段，是保证金与资金安全模型的主依据。",
        1044: "补充说明：在保证金与垫资口径上，DMP 负责“约定是否存在、方式是什么、预估金额是多少”，SAP 负责“实际是否发生、何时回收、是否逾期”，审计附表只保留 DMP 和 SAP 都无法覆盖的例外说明。方案后续如继续精简字段，应优先剔除附表中与 SAP 重复的保证金、预收款和累计收款字段。",
    }

    for idx, text in updates.items():
        set_text(doc, idx, text)

    doc.save(str(dst))
    print(dst.resolve())


if __name__ == "__main__":
    main()
